# Dependencies (reinstalled inside Docker)
node_modules
.turbo

# Build outputs (rebuilt inside Docker)
**/dist
**/dist-electron

# Version control
.git

# IDE / editor
.vscode
.cursor

# Railway instruction docs (not needed in image)
railway-instruction/

# Desktop app (not needed for server deployment)
apps/desktop/

# Marketing site (not needed for server deployment)
apps/marketing/
